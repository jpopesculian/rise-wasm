{
  "scripts": {
    "build-rust": "wasm-pack build --target nodejs --dev",
    "build-wasm": "wat2wasm test.wat",
    "run-test": "node -e \"require('./test')\"",
    "all": "yarn build-rust && yarn build-wasm && yarn run-test"
  },
  "devDependencies": {},
  "dependencies": {
    "bip39": "^2.5.0",
    "bitcoinjs-lib": "^4.0.3"
  }
}
